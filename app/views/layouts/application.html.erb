<!DOCTYPE html>
<html>
<head>
  <title>TicTacToe Intensive</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="header">
    <% if current_player %>
      Logged in as <%= current_player.name %>
    <% end %>

    <% flash.each do |name, message| %>
      <div class="alert-box <%= name %>">
        <%= message %>
      </div>
    <% end %>
    <nav>
      <ul class ="nav">
        <% if current_player %>
          <li><%= link_to 'Log out', session_path(session), method: :delete, data: { confirm: 'Are you sure?' } %></li>
        <% else %>
          <li><%= link_to('Log in', login_path) %></li>
          <li><%= link_to('Create player profile', new_player_path)  %></li>
        <% end %>
        </ul>
      </nav>
    </div>
  </div>
  <div class="container">
    <%= yield %>
  </div>
  <div class="footer">
    <br>
    <center>Made by Cee in week 5 WDI-LDN-2014</center>
    <br>
  </div>
</body>
</html>
